import React from "react";

const BoxAuth = ({title}) => {
  return (
    <div className="w-1/2 bg-blue-600 text-gray-100 text-5xl font-bold h-full items-center justify-center flex">
        {title}
    </div>
  );
};

export default BoxAuth;
